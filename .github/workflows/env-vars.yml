name: Environment Variables
on: [push]
jobs:
  env-vars-and-context:
    runs-on: ubuntu-22.04
    steps:
      - name: Log ENV VAR
        run: echo $GITHUB_REF
      - name: Log Context
        run: echo '${{ github.ref }}'
